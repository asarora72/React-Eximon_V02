name: Deploy landing page

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:   
    - uses: actions/checkout@v3
    - name: Deploy react app to github pages
      uses: ./.github/actions/git@github.com:asarora72/React-Eximon@master
